<%= form_for(current_user.supports.build(idea_id: idea.id)) do |f| %>
<div><%= hidden_field_tag :user_id, user.id %></div>
<div><%= hidden_field_tag :idea_id, idea.id %></div>
  <%= f.submit "Add my Voice", class: "btn btn-primary" %>
<% end %>
