<%= form_for(@idea) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: "Share your ideas with Victoria" %>
  </div>
  <%= f.submit "Share Idea", class: "btn btn-primary" %>
<% end %>
