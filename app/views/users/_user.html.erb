<p>user partial</p>
<% if not @user.nil? %>
  <%= link_to gravatar_for(@user, size: 50), current_user %>
  <h1><%= @user.first_name %></h1>
  <span><%= link_to "#{@user.first_name}", @user %></span>
  <span><%= pluralize(@user.ideas.count, "idea") %></span>
<% else %>
  <p>nil user</p>
<% end %>
